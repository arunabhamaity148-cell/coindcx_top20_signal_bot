# ============================================================================
# Runtime dependencies - CoinDCX TOP-20 SIGNAL-ONLY futures intelligence bot
# Python 3.11+ (developed and tested on CPython 3.11/3.12)
# Ranges (not exact pins) keep the file installable across patch releases; the
# full lock for a reproducible deployment should be generated with
#   pip freeze > requirements.lock
# ============================================================================

aiohttp>=3.9,<4.0
aiofiles>=23.2
httpx>=0.27
feedparser>=6.0.11
PyYAML>=6.0
pydantic>=2.7
numpy>=1.26

# ---------------------------------------------------------------------------
# DEVELOPMENT / TEST ONLY (safe to omit on a production host)
# ---------------------------------------------------------------------------
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.5
black>=24.4
